<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="studios.fyhrf.hairdew">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainLogin"></activity>
        <activity android:name=".CreateActivity"></activity>
        <activity android:name=".ExploreActivity"></activity>
        <activity android:name=".FAQActivity"></activity>
        <activity android:name=".BuildProductActivity"></activity>
        <activity android:name=".SubmitActivity"></activity>
        <activity android:name=".ChooseBaseInfoActivity"></activity>
        <activity android:name=".ChooseOilInfoActivity"></activity>
        <activity android:name=".ChooseScentInfoActivity"></activity>

    </application>

</manifest>